# Turborepo starter
```shell
npx create-turbo@latest
# choose pnpm
```

# Set storybook
```shell
mkdir ./packages/common-ui
cd ./packages/common-ui
pnpm init
pnpm add storybook
pnpx init storybook
pnpx add @storybook/cli
```
